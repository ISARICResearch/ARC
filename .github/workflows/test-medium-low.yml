name: Run tests marked as medium or low

on:
  workflow_dispatch:

jobs:
  build:
    name: Tests (low/medium)
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v6

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: install
        run: |
          python --version
          python -m pip install --upgrade pip
          pip install ".[dev]"

      - name: run unit tests
        run: |
          pytest -q -m "medium or low"
